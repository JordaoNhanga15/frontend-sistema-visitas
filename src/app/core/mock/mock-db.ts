import { VISITORS_MOCK } from './visitors.mock';
import { VISITS_MOCK } from './visits.mock';
import { USERS_MOCK } from './users.mock';

export const MockDB = {
  visitors: VISITORS_MOCK,
  visits: VISITS_MOCK,
  users: USERS_MOCK
};

